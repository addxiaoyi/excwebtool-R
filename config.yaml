current_site: site1

sites:
  site1:
    name: 微信后台示例
    login_url: "https://example.com/login"
    target_url: "https://example.com/table"
    username: "填写账号"
    password: "填写密码"

    fields:
      订单号: order_id
      用户名: username
      金额: amount
      时间: create_time

    pagination:
      type: page_number   # 页码型
      max_pages: 999

    excel:
      file: output.xlsx
      sheet: 数据
      deduplicate_by: order_id
